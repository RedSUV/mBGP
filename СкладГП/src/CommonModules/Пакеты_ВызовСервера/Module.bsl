Функция СвойстваПакетаМоб(Пакет) Экспорт
	Возврат Пакеты.СвойстваПакетаМоб(Пакет);
КонецФункции

Функция ТекущееНазначениеПакета(Пакет) Экспорт
	ВыборкаПервой = Справочники.Ячейки.Выбрать(, ОбщегоНазначенияВызовСервераПовтИсп.Склад());
	Пока ВыборкаПервой.Следующий() Цикл
		Если Не ВыборкаПервой.ЭтоГруппа Тогда
			Возврат ВыборкаПервой.Ссылка;
		КонецЕсли;
	КонецЦикла;
	
КонецФункции
